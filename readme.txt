# 1. Install Git if you don't have it: https://git-scm.com/downloads

# 2. Open terminal / command prompt

git clone https://github.com/yourusername/dtcm-tool.git
cd dtcm-tool

# 3. Put your index.html file into this folder

git add index.html
git commit -m "Add DTCM Reconciliation Tool"
git push origin main